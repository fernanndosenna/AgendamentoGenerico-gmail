<!--exibindo os detalhes de eventos após o click no calendário-->
<h1>Consulta: <%= appo.description %> </h1>
<hr>
<h4>Paciente: <%= appo.name %> </h4>
<h4>Email: <%= appo.email %> </h4>
<h4>CPF: <%= appo.cpf %> </h4>
<hr>
<% if(!appo.finished) { %> <!--caso a consulta não esteja finalizado ainda, então, o botão será exibido -->
    <form action="/finish" method="post">
        <input type="hidden" name="id" value="<%= appo._id %>"/>
        <button>Finalizar consulta</button>
    </form>

<% }%>